version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.16
  command: python3 llmrag/webserver.py
  network:
    port: 8080
